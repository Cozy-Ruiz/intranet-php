replicaCount: 1

appEnv: "DEV"

image:
  tag: "1.8"

ingress:
  enabled: true
  className: nginx
  host: intranet.local

autoscaling:
  enabled: true
  minReplicas: 1
  maxReplicas: 5
  targetCPUUtilizationPercentage: 60