replicaCount: 1

appEnv: "PROD"

image:
  tag: "1.8"

ingress:
  enabled: true
  className: nginx
  host: intranet.prod

autoscaling:
  enabled: true
  minReplicas: 1
  maxReplicas: 10
  targetCPUUtilizationPercentage: 60